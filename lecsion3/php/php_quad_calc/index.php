<?php

$a = $_GET['a'] ?? 4;
$b = $_GET['b'] ?? 12;
$c = $_GET['c'] ?? 4;

//вычисляем дискриминант
$d = $b * $b - 4 * $a * $c;

//вычисляем x1 и x2 с округлением (функция round) до десятых
$x1 = round(($b * (-1) + sqrt($d)) / (2 * $a), 1);
$x2 = round(($b * (-1) - sqrt($d)) / (2 * $a), 1);

include "template.php";
